{
  "name": "gmaps-scrapper",
  "version": "0.0.1",
  "description": "Google maps scrapper",
  "license": "ISC",
  "author": "napstar-420",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently \"pnpm run dev --prefix server\" \"npm run dev --prefix client\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
